# Make C-b r
unbind r
bind r source-file ~/.tmux.conf

set -g default-terminal "tmux-256color"


set-option -g set-clipboard on
set-option -g status-position bottom

set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'catppuccin/tmux'
set -g @plugin 'tmux-plugins/tmux-cpu'

set -g status-left ""
set -g status-right "#{E:@catppuccin_status_application} #{E:@catppuccin_status_session}"
set -g @catppuccin_window_status_style "rounded"


run '~/.config/tmux/plugins/catppuccin/tmux/catppuccin.tmux'

# Make the status line pretty and add some modules
set -g status-right-length 100
set -g status-left-length 100
set -g status-left ""
set -g status-right "#{E:@catppuccin_status_application}"
set -agF status-right "#{E:@catppuccin_status_cpu}"
set -ag status-right "#{E:@catppuccin_status_session}"
set -ag status-right "#{E:@catppuccin_status_uptime}"
set -agF status-right "#{E:@catppuccin_status_battery}"

#set-option -g window-status-current-format '#I:#[fg=#{@thm_crust},bg=#{@thm_teal}]#{window_name}#F'
#set-option -g window-status-format '#I:#[fg=#{@thm_fg},bg=#{@thm_mantle}#{window_name}#F'
#set-option -g status-interval 1
# windows
#set -g @catppuccin_window_default_text " #{window_name}"
#setw -g window-status-format '#[fg=#002b36,bg=#002b36]#[default] #I  #{=10:window_name} #[fg=#002b36,bg=#002b36,nobold,nounderscore,noitalics]'
#setw -g window-status-current-format '#[fg=#002b36,bg=#eee8d5]#[fg=#b58900,bg=#eee8d5] #I #[fg=#eee8d5,bg=#b58900] #{=10:window_name} #[fg=#b58900,bg=#002b36,nobold]'
#setw -g window-status-current-format "#{E:@catppuccin_status_application} #{=10:window_name} "

#run '~/.tmux/plugins/tmux/tests/cpu_module.sh'
#run ~/.config/tmux/plugins/tmux-plugins/tmux-cpu/cpu.tmux
#run ~/.config/tmux/plugins/tmux-plugins/tmux-battery/battery.tmux
run '~/.tmux/plugins/tpm/tpm'

##### - Odin

#setw -g mouse on

# plugins
#set -g @plugin 'tmux-plugins/tpm'
#set -g @plugin 'tmux-plugins/tmux-sensible'
#set -g @plugin 'dracula/tmux'
#set -s default-terminal 'xterm-256color'

# dracula customizations
#set -g @dracula-plugins "battery weather time"
#set -g @dracula-show-powerline true
#set -g @dracula-show-fahrenheit false
#set -g @dracula-military-time true

#run '~/.tmux/plugins/tpm/tpm'

